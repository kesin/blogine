!!!
%html{:lang => "zh-CN"}
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %meta{:charset => "utf-8"}/
    %meta{:content => "IE=Edge,chrome=1", "http-equiv" => "X-UA-Compatible"}/
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}/
    %meta{name: 'Description',content: content_for?(:meta_description) ? yield(:meta_description) : 'Blogine 后台管理'}
    %meta{name: 'Keywords',content: content_for?(:meta_keywords) ? yield(:meta_keywords) : 'Blogine 后台管理'}
    %title= content_for?(:title) ? "#{yield(:title)} - #{'后台管理'}" : '后台管理'
    = csrf_meta_tags
    / Le HTML5 shim, for IE6-8 support of HTML elements
    /[if lt IE 9]
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    = stylesheet_link_tag "application", :media => "all"
    = stylesheet_link_tag "admin/application", :media => "all"
    / For all other devices
    / Size should be 32 x 32 pixels
    = favicon_link_tag 'favicon.ico', :rel => 'shortcut icon'
    = javascript_include_tag 'application'
  %body
    = render 'layouts/flash'
    #wrapper
      = render 'layouts/admin_menu'
      #page-wrapper
        = yield