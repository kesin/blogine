%table
  %thead
    %tr
      %th Title
      %th Column
      %th Status
      %th
      %th
      %th

  %tbody
    - @posts.each do |post|
      %tr
        %td= post.title
        %td= post.column.try(:name)
        %td= post.status
        %td= link_to 'Show', post
        %td= link_to 'Edit', edit_post_path(post), 'data-turbolinks' => false
        %td= link_to 'Destroy', post, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Post', new_post_path
